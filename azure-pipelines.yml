# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

# variables:
#   GLOBAL_SUB_ID: $(sub_id)

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # Write your commands here
      echo 'Hello world'
      zip -r test-code.zip app.py requirements.txt
      cp test-code.zip $(System.DefaultWorkingDirectory)/code.zip

- task: AzureRmWebAppDeployment@4
  inputs:
    ConnectionType: 'AzureRM'
    azureSubscription: 'Azure subscription 1(de46afed-d1b8-4bd8-a8ad-e0f0b1235943)'
    appType: 'webAppLinux'
    WebAppName: 'sid-test-app'
    packageForLinux: '$(System.DefaultWorkingDirectory)/code.zip'
    RuntimeStack: 'PYTHON|3.8'
